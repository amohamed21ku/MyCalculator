<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Button styles -->
    <style name="buttonRow">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
    </style>

    <style name="buttonNumber">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@drawable/rounded_button</item> <!-- Rounded button background -->
        <item name="android:textSize">25sp</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="buttonOperator">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@drawable/rounded_button</item> <!-- Rounded button background -->
        <item name="android:textSize">25sp</item>
        <item name="android:textColor">@color/yellow</item>
    </style>

    <!-- Rounded button background drawable -->
    <drawable name="rounded_button">
        <shape xmlns:android="http://schemas.android.com/apk/res/android"
            android:shape="rectangle">
            <corners android:radius="10dp" /> <!-- Adjust the radius for more/less rounded corners -->
            <solid android:color="@color/button_background" /> <!-- Define the background color -->
        </shape>
    </drawable>
</resources>
